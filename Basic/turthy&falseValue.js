const myArray = [];
if (myArray.length === 0) {
    console.log("Empty array");
}

const  myObject = {};
if(Object.keys(myArray).length === 0) { // Object.keys() returns an array
    console.log("Empty object");
}